# to_emoji
🤖✨ Twitter bot that translates every tweet you send it to emoji


## Usage
Populate the `.env` file like this:
```
module.exports = {
  consumer_key:         '',
  consumer_secret:      '',
  access_token:         '',
  access_token_secret:  ''
}
```
